spring.application.name=maverick-bank
server.port=5000

# Database
spring.datasource.url=jdbc:mysql://${DB_HOST:localhost}:3306/MaverickBank?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=Siva@12345

spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Eureka Clientt
eureka.client.service-url.defaultZone=http://localhost:8761/eureka/
eureka.client.register-with-eureka=true
eureka.client.fetch-registry=true

eureka.instance.hostname=localhost
eureka.instance.instance-id=${spring.application.name}:${server.port}